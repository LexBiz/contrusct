import { PublicShell } from "@/components/PublicShell";
import { PrivacyPage } from "@/components/PublicPages";

export default function PrivacyRootPage() {
  return (
    <PublicShell locale="cs">
      <PrivacyPage locale="cs" />
    </PublicShell>
  );
}
