import { PublicShell } from "@/components/PublicShell";
import { ServicesPage } from "@/components/PublicPages";

export default function ServicesRootPage() {
  return (
    <PublicShell locale="cs">
      <ServicesPage locale="cs" />
    </PublicShell>
  );
}
