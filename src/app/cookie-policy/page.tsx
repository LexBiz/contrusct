import { PublicShell } from "@/components/PublicShell";
import { CookiePolicyPage } from "@/components/PublicPages";

export default function CookiePolicyRootPage() {
  return (
    <PublicShell locale="cs">
      <CookiePolicyPage locale="cs" />
    </PublicShell>
  );
}
